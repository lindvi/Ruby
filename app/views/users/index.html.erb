<div class="page-header">
  <h1 class="hleft">Användare</h1>
</div>
<table class="table table-condensed">
	<thead>
		<tr>
			<th>Phone</th>
			<th>Admin</th>
			<th>Controllers</th>
		</tr>
	</thead>
	<tbody>
	<% @users.each do |user| %>
		<tr>
			<td><%= user.phone %></td>
			<td><%= user.admin? %></td>
			<td><%= link_to "Destroy", user_path(user), method: :delete, data: { confirm: "You sure?" },  class:"btn btn-small btn-danger" %>
			</td>
		</tr>
	<% end %>
	</tbody>	
</table>