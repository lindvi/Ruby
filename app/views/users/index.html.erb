<div class="page-header">
  <h1 class="hleft">Användare</h1>
</div>
<table class="table table-condensed">
	<thead>
		<tr>
			<th>#</th>
			<th>Phone</th>
			<th>Admin</th>
			<th>Controllers</th>
		</tr>
	</thead>
	<tbody>
	<% @users.each do |user| %>
		<tr>
			<td><%= user.id %></td>
			<td><%= user.phone %></td>
			<td><%= user.admin? %></td>
			<td>
				<%= link_to "Show", user, class:"btn btn-small btn-success" %>
				<%= link_to "Destroy", user_path(user), method: :delete, data: { confirm: "You sure?" },  class:"btn btn-small btn-danger" %>
			</td>
		</tr>
	<% end %>
	</tbody>	
</table>